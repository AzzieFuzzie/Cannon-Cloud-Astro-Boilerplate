paths:
  static: public
  uploads: public/uploads
timezone: Africa/Johannesburg
markdown:
  engine: commonmark
  options:
    gfm: true
collections_config:
  pages:
    path: src/pages
    icon: wysiwyg
    url: "/[slug]/"
    glob:
      - "*.astro"
    _enabled_editors:
      - visual
    disable_add: true
 